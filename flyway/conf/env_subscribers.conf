# Subscribers DB Flyway config
flyway.url=${FLYWAY_URL}
flyway.user=${FLYWAY_USER}
flyway.password=${FLYWAY_PASSWORD}
flyway.locations=filesystem:/flyway/subscribers_sql
flyway.baselineOnMigrate=true


